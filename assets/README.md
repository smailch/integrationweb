Dépose le logo ici avec le nom:

- `logo-esprit-club.jpg`

Ensuite, il sera affiché automatiquement dans la sidebar (Dashboard + Liste Evenement).

Si ton fichier est en PNG, soit:
- renomme-le en `logo-esprit-club.jpg`, ou
- dis-moi le nom exact et je mets à jour le `src` dans les pages.
